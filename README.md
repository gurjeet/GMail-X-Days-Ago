# GMail X Days Ago - WebExtension

## What it Does

This browser extension adds an icon to the address bar. The button shows a
popup dialog box, which allows the user to navigate to emails that are some days
in past.

